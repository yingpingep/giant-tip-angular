import { TempDirective } from './temp.directive';

describe('TempDirective', () => {
  it('should create an instance', () => {
    const directive = new TempDirective();
    expect(directive).toBeTruthy();
  });
});
