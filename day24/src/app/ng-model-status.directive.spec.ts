import { NgModelStatus } from './ng-model-status.directive';

describe('NgModelStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new NgModelStatus();
    expect(directive).toBeTruthy();
  });
});
