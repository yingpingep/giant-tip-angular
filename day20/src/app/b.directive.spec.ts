import { BDirective } from './b.directive';

describe('BDirective', () => {
  it('should create an instance', () => {
    const directive = new BDirective();
    expect(directive).toBeTruthy();
  });
});
